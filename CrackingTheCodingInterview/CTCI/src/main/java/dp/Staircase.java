package dp;

/**
 * A child is running up a staircase with n steps and can hop either 1 step or 2 steps, or 3
steps at a time. Implement a method to count how many possible ways the child can run up the
stairs.
 * Problem dimension: 1
 * int DP[position]
 * Transitions: 1 and 2
 *
 * This is exactly the same as Fibonacci.
 *
 * What about at most K steps?
 */
public class Staircase {
	static public int calculate(int N) {
		Integer steps = 0;
		return steps;
	}
}
